\begin{table}[ht]
\centering
\caption{Out-of-sample validation of the early-warning system on the
\textbf{full 100-tag panel}. Signals are computed from observed counts
through end-2024 and used to predict each tag's \emph{realised 2025}
high-value erosion (held out). The activity-decline signal is an
excellent detector of where high-value erosion will occur; the
value-disproportionality \emph{ratio} is near chance, which is why the
system detects on activity (extensive margin) and uses the value-margin
estimates only to calibrate magnitude (intensive margin, see
Table~\ref{tab:vtd_voi}). The composition flag itself is highly
persistent across years.}
\label{tab:vtd_validation}
\small
\begin{tabular}{lr}
\toprule
Quantity (full 100-tag panel) & Value \\
\midrule
AUC, activity decline $\to$ top-tercile 2025 HV erosion & 0.98 \\
AUC, value-disproportionality ratio $\to$ same & 0.57 \\
\midrule
Persistence: Spearman(HV drop 2024, HV drop 2025) & 0.90 ($p<0.001$) \\
\bottomrule
\end{tabular}
\end{table}
